export const getTotalWorkTime = (from: Date, to: Date) => {
  //     const diffTime = Math.abs(to.toLocaleString() - from.toLocaleString());
  // return Math.ceil(diffTime / (1000 * 60 * 60 * 24));
  return to;
};
