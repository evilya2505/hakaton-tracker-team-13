import { Container } from "@mui/material";

const Statistics: React.FC<{}> = (): JSX.Element => {
  return (
    <Container>
      <h2>Статистика</h2>
    </Container>
  );
};

export default Statistics;
